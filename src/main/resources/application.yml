server:
  port: 7777
  # 0 - Random Port

spring:
  application:
    name: user-service
  zipkin:
    base-url : http://127.0.0.1:9411
    enabled: true
  sleuth:
    sampler:
      probabilitiy: 1.0

#  rabbitmq:
#    host: 127.0.0.1
#    port: 5672
#    username: guest
#    password: guest
  h2:
    console:
      enabled: true
      settings:
        web-allow-others: true
      path: /h2-console
#  datasource:
#    driver-class-name: org.h2.Driver
#    url: jdbc:h2:mem:testdb

eureka:
  instance:
    instance-id: ${spring.application.name}:${spring.application.instacne_id:${random.value}}
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka

greeting:
  message: Welcome to the Simple E-commerce

logging:
  level:
    com.example.userservice: DEBUG

management:
  endpoints:
    web:
      exposure:
        include: refresh, health, beans #, busrefresh
#token:
#  expiration_time: 86400000 # 1일짜리 토큰 (60(s) * 60(m) * 24(h) * 1000(ms))
#  secret: user_token